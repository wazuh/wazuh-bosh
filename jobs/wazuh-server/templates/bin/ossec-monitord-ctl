#!/bin/bash

set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Setup env vars and folders for the webapp_ctl script
source /var/vcap/jobs/wazuh-server/helpers/ctl_setup.sh 'wazuh-server' 'ossec-monitord'

case $1 in

  start)
    pid_guard $PIDFILE $JOB_NAME

    # store pid in $PIDFILE
    echo $$ > $PIDFILE

    exec /var/vcap/store/wazuh-server/bin/ossec-monitord -f \
         -u root -g root \
         -c /var/vcap/jobs/wazuh-server/config/ossec.conf \
         -D /var/vcap/store/wazuh-server \
         >>$LOG_DIR/$JOB_NAME.monitord.log 2>&1

    ;;

  stop)
    kill_and_wait $PIDFILE

    ;;
  *)
    echo "Usage: ossec-monitord-ctl {start|stop}"

    ;;

esac
exit 0
